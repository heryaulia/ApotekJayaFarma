<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- apotek.person tree view -->
    <record id="apotek_person_view_tree" model="ir.ui.view">
        <field name="name">apotek.person.view.tree</field>
        <field name="model">apotek.person</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="no_hp"/>
                <field name="alamat"/>
                <field name="tgl_lahir"/>
            </tree>
        </field>
    </record>

    <!-- apotek.person form view -->
    <record id="apotek_person_view_form" model="ir.ui.view">
        <field name="name">apotek.person.view.form</field>
        <field name="model">apotek.person</field>
        <field name="arch" type="xml">
            <form string="Nama">
                <sheet>
                    <group name="nama" string="Nama">
                        <field name="name"/>
                    </group>
                    <group name="data" string="Data">
                        <field name="no_hp"/>
                        <field name="alamat"/>
                        <field name="tgl_lahir"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>